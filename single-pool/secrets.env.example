# Node secrets, shared by every node in the pool.
#
# Copy this file into the pool/ stack directory you run `terraform apply` from
# as `secrets.env` and fill in real values:
#
#   cp ../secrets.env.example secrets.env
#
# In ssh mode it is uploaded to each node over SSH at provision time and merged
# over the baked mgx-env (secrets win). It is git-ignored and never baked into
# the AMI.

# Cassandra username / password
CASS_USER=<CASS_USER>
CASS_PASSWD=<CASS_PASSWD>

# mgx gateway / node X-API keys.
MGX_GW_X_API_KEY="<X_API_KEY>"
MGX_X_API_KEY="<X_API_KEY>"

# mgx gateway admin password.
MGX_GW_ADMIN_PASSWD=<MGX_GW_ADMIN_PASSWD>

# Grafana admin user / password (used when metrics are enabled).
GRAFANA_USER=<GRAFANA_USER>
GRAFANA_PASSWD=<GRAFANA_PASSWD>
